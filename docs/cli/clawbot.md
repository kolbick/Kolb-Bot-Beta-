---
summary: "CLI reference for `kolb-bot clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `kolb-bot clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `kolb-bot clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `kolb-bot clawbot qr` (same behavior as [`kolb-bot qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `kolb-bot clawbot qr` -> `kolb-bot qr`
