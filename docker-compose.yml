services:
  kolb-bot-gateway:
    image: ${KOLB_BOT_IMAGE:-kolb-bot:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      KOLB_BOT_GATEWAY_TOKEN: ${KOLB_BOT_GATEWAY_TOKEN:-}
      KOLB_BOT_ALLOW_INSECURE_PRIVATE_WS: ${KOLB_BOT_ALLOW_INSECURE_PRIVATE_WS:-}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${KOLB_BOT_CONFIG_DIR}:/home/node/.kolb-bot
      - ${KOLB_BOT_WORKSPACE_DIR}:/home/node/.kolb-bot/workspace
      ## Uncomment the lines below to enable sandbox isolation
      ## (agents.defaults.sandbox). Requires Docker CLI in the image
      ## (build with --build-arg KOLB_BOT_INSTALL_DOCKER_CLI=1) or use
      ## docker-setup.sh with KOLB_BOT_SANDBOX=1 for automated setup.
      ## Set DOCKER_GID to the host's docker group GID (run: stat -c '%g' /var/run/docker.sock).
      # - /var/run/docker.sock:/var/run/docker.sock
    # group_add:
    #   - "${DOCKER_GID:-999}"
    ports:
      - "${KOLB_BOT_GATEWAY_PORT:-18789}:18789"
      - "${KOLB_BOT_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${KOLB_BOT_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  kolb-bot-cli:
    image: ${KOLB_BOT_IMAGE:-kolb-bot:local}
    network_mode: "service:kolb-bot-gateway"
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      HOME: /home/node
      TERM: xterm-256color
      KOLB_BOT_GATEWAY_TOKEN: ${KOLB_BOT_GATEWAY_TOKEN:-}
      KOLB_BOT_ALLOW_INSECURE_PRIVATE_WS: ${KOLB_BOT_ALLOW_INSECURE_PRIVATE_WS:-}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
    volumes:
      - ${KOLB_BOT_CONFIG_DIR}:/home/node/.kolb-bot
      - ${KOLB_BOT_WORKSPACE_DIR}:/home/node/.kolb-bot/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - kolb-bot-gateway
